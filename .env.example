# MetaTrader 5 broker account
# The originals had these as literals in the first cell of every notebook.
# They have been replaced by os.environ[...] reads and the values rotated.
MT5_LOGIN=00000000
MT5_PASSWORD=
# server was 'Alpari-MT5-Demo' and is left in the source as a literal

# Outbound alert mail (SMTP_SERVER / SMTP_PORT stay in the source)
EMAIL_ADDRESS=
EMAIL_PASSWORD=

# Where retrain reports and error alerts are sent
ALERT_RECIPIENT=
